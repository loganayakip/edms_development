<!DOCTYPE html>
<html>
<head>
 <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Edms Development" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Edms Development" %>">
    <%= stylesheet_link_tag 'simple', 'scaffold','jquery-ui',media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  <script src="jquery-ui.js"></script>
  <script src="jquery-1.10.2.js"></script>
  <script>
  $(function() {
  $( "#datepicker" ).datepicker({ 
      dateFormat:'yy-dd-mm', 
      dateonly:true,
      changeMonth:true,
      changeYear:true,
      changeDate:true
  }); 
  });
  </script>
</head>
<body>
  
 <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </main>
  </body>
</html>
